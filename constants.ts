export const APP_NAME = "旅点 TripSpot";
export const MAP_CONTAINER_ID = "amap-container";

// NOTE: In a real production app, these should be environment variables.
// Users must replace these with their own keys.
export const AMAP_KEY = "YOUR_AMAP_JS_KEY"; 
export const AMAP_SECURITY_CODE = "YOUR_AMAP_SECURITY_CODE";

export const SAMPLE_PROMPT = `一定要去南桥看蓝眼泪，还有陶德砂锅（春熙路那家排队少）。如果是下午去，可以顺路去一下杜甫草堂。`;